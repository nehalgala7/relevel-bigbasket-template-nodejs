const express = require("express");
const router = express.Router();
const AddressController = require('../controllers/address.controller');

// Create routes for address here
module.exports = router;